import 'package:flutter/material.dart';

class AppBorders {
  static const borderL = BorderRadius.all(
    Radius.circular(50),
  );

  static const borderM = BorderRadius.all(
    Radius.circular(20),
  );

  static const borderS = BorderRadius.all(
    Radius.circular(10),
  );
}
